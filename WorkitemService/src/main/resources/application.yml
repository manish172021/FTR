server:
  port: 8004

spring:
  datasource:
    url: jdbc:mysql://${DB_HOST:localhost}:3306/workitemdb
    username: root
    password: root
    driverClassName: com.mysql.cj.jdbc.Driver
  jpa:
    database-platform: org.hibernate.dialect.MySQLDialect
    hibernate:
      ddl-auto: update
